# Sessao 2026-08-28 18:36 — promoção ao mestre

- **Tipo**: trabalho no pacote, não carga de cliente. `projeto.nome`: nenhum.
- **Provedor**: Claude Opus 5 (Claude Code)
- **Versões publicadas**: v1.0.0 → v1.1.0 → v1.2.0 → v1.2.1

## Decisões
- Repo publicado no GitHub, privado, com o pacote na raiz — `AGENTS.md` no topo, qualquer harness lê ao clonar.
- Formato mantido: pasta de markdown lida via `AGENTS.md`. Skill ou plugin de IDE seria mais fácil de instalar e mataria o requisito multi-harness.
- Privacidade virou regra dura (regra 11): nenhum arquivo do mestre nomeia cliente, base ou carga. A pasta de casos por cliente deixou de existir; a lição sobe anônima ou não sobe.
- Versionamento por carga (regra 12): uma carga roda numa tag fixa, porque atualizar o método no meio invalida validação já rodada.
- Automação da grade de entrada **não** foi construída: exigiria fixar o formato da planilha fiscal sem ter uma em mãos. Fica para quando houver arquivo real.

## Promovido (método)
- `grade-unica.md` — filtro e numeração num único leitor; camadas divergentes só quebram no Q27, depois do INSERT.
- `vigencia-tcd3.md` — TCD3 é 1:N com TCD2; sobreposição de períodos torna a determinação ambígua.
- `templates/projeto/` — esqueleto do repo de carga, que a doutrina exigia sem definir.
- `VERSION`, `CHANGELOG.md`, bump obrigatório no `evoluir`.

## Corrigido no método
- `gerar_tcd3.py` ignorava `skip_prioridades` — gerava `Tcd2Id` para AbsId que nunca entrou na TCD2.
- TCD3 de produção não existia (levantava erro); agora 1:1 pela grade ou N:1 por `TCD3_CARGA.xlsx`.
- Lotes passaram a valer nas três camadas, não só na TCD5.
- Q27 afirmava `abertos = total = COUNT TCD2`; com N vigências o total é maior. Rodapé e `context/validacao.md` corrigidos.

## Observado
- `python -m unittest discover -s tests` — 17 testes, OK, inclusive em clone limpo da tag publicada.
- Nenhum COUNT de SAP: não houve carga nesta sessão.

## Pendências
- Montagem de `TCD2_CARGA.xlsx` / `TCD5_CARGA.xlsx` a partir da planilha fiscal continua manual (candidato a v1.3.0, com planilha real em mãos).
