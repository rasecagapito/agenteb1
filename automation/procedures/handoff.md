# Procedimento: HANDOFF

Arquivo: `memory/handoff.md`. ≤ 20 linhas. Substituir, nunca acumular.

Campos: IA, quando, `projeto.nome` (ou nenhum), camada, feito, bloqueio, próxima intenção.

Se `projeto.nome` vazio, a próxima IA não inventa cliente.
