# Procedimento: STATUS

Ler `config.yaml` (`projeto.nome`), `memory/handoff.md`, camadas em `context/carga-tcd.md`.

Responder:
- Nome do projeto atual (ou “nenhum”)
- Camada (TCD1/2/3/5) e se a UI já Atualizou
- Bloqueio de lookup aberto
- Próximo passo **deste** nome, sem misturar outro cliente
